{
  "id": "root-layer-system-04",
  "seq": "04",
  "title": "Viewport: verify Scene is observer() and invalidation is correct — apply minimal fix if needed",
  "status": "pending",
  "depends_on": ["01", "02", "03"],
  "parallel": false,
  "suggested_agent": "CoderAgent",
  "context_files": ["C:\\Users\\Dan\\.opencode\\context\\core\\standards\\code-quality.md"],
  "reference_files": [
    "src/renderer/src/components/Viewport/Viewport.tsx",
    "src/renderer/src/stores/ProjectStore.ts"
  ],
  "acceptance_criteria": [
    "Confirm Scene component in Viewport.tsx is already wrapped with observer() from mobx-react-lite — if so, document this finding in the completion_summary and make no code change",
    "If Scene is NOT observer(), wrap it with observer() so MobX layer mutations automatically trigger re-renders",
    "With frameloop='demand', verify that store mutations from PanHandle/ResizeHandle callbacks (which call projectStore.updateLayer()) cause the canvas to re-render: because Scene is observer() and reads projectStore.layers, MobX will call invalidate automatically via the R3F observer integration",
    "If automatic invalidation is NOT working (canvas stays stale after drag), add a minimal useEffect inside Scene that calls invalidate() in response to layer position/size changes using a MobX reaction or autorun — document the chosen approach",
    "bun run typecheck passes with no new errors"
  ],
  "deliverables": [
    "src/renderer/src/components/Viewport/Viewport.tsx — observer() confirmed or applied; invalidation fix applied only if needed"
  ],
  "started_at": null,
  "completed_at": null,
  "completion_summary": null
}
